
{
  "name": "career-connect-mock-backend",
  "version": "1.0.0",
  "description": "A mock API server for the Career Connect frontend.",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3001 --routes routes.json"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
